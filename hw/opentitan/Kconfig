# OpenTitan devices

config OT_AES
    select OT_PRNG
    bool

config OT_ALERT
    bool

config OT_AST
    bool

config OT_CLKMGR
    bool

config OT_CSRNG
    bool

config OT_EDN
    bool

config OT_ENTROPY_SRC
    bool

config OT_FLASH
    bool

config OT_HMAC
    bool

config OT_IBEX_WRAPPER
    bool

config OT_LIFECYCLE
    bool

config OT_OTBN
    bool
    select OPENTITAN_BIGNUMBER

config OT_OTP
    bool

config OT_PINMUX
    bool

config OT_PRNG
    bool

config OT_PWRMGR
    bool

config OT_SENSOR
    bool

config OT_SPI_HOST
    bool
    select SSI

config OT_TIMER
    bool

config OT_UART
    bool

source otbn/Kconfig
